# Unit Sprite Variants

This folder preserves selected character-plus-vehicle unit concepts.

Use these for committed art-direction history when a generated concept is worth keeping but is not ready to replace a runtime alias in `public/assets`.

Runtime sprites still use stable aliases from `public/assets`. Promote a unit variant into runtime only after explicit selection, cleanup, chroma-key removal, sizing, and gameplay verification.
